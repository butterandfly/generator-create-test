language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-create-test' ]; then cd .. && eval "mv $currentfolder generator-create-test" && cd generator-create-test; fi
